import { StyleSheet } from 'react-native';

export const authStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    textAlign: 'center',
    padding: 5,
  },
  formInput: {},
  formPicker: {},
});
